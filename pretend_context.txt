
    Γçé create index "train
ing_modules_is_active_index
" on "training_modules" 
("is_active")  
    Γçé create unique 
index "training_modules_slu
g_unique" on 
"training_modules" 
("slug")  
    Γçé create table 
"user_training_progress" 
("id" integer primary key 
autoincrement not null, 
"user_id" integer not 
null, "training_module_id" 
integer not null, 
"completion_percentage" 
integer not null default 
'0', "status" varchar 
check ("status" in 
('locked', 'available', 
'in_progress', 
'completed')) not null 
default 'available', 
"last_accessed_at" 
datetime, "created_at" 
datetime, "updated_at" 
datetime, foreign 
key("user_id") references 
"users"("id") on delete 
cascade, foreign 
key("training_module_id") 
references 
"training_modules"("id") 
on delete cascade)  
    Γçé create unique 
index "user_training_progre
ss_user_id_training_module_
id_unique" on 
"user_training_progress" 
("user_id", 
"training_module_id")  
    2026_03_11_043003_creat
e_assessment_recovery_table
s   
>   Γçé select exists 
(select 1 from 
"main".sqlite_master where 
name = 
'assessment_attempts' and 
type = 'table') as 
"exists"  
>   Γçé create table 
"assessment_attempts" 
("id" integer primary key 
autoincrement not null, 
"created_at" datetime, 
"updated_at" datetime)  
>   Γçé select name, type, 
not "notnull" as 
"nullable", dflt_value as 
"default", pk as 
"primary", hidden as 
"extra" from pragma_table_x
info('assessment_attempts',
 'main') order by cid asc  
>   Γçé select "sql" from 
"main".sqlite_master where 
type = 'table' and name = 
'assessment_attempts'  
>   Γçé select name, type, 
not "notnull" as 
"nullable", dflt_value as 
"default", pk as 
"primary", hidden as 
"extra" from pragma_table_x
info('assessment_attempts',
 'main') order by cid asc  
>   Γçé select "sql" from 
"main".sqlite_master where 
type = 'table' and name = 
'assessment_attempts'  
>   Γçé select name, type, 
not "notnull" as 
"nullable", dflt_value as 
"default", pk as 
"primary", hidden as 
"extra" from pragma_table_x
info('assessment_attempts',
 'main') order by cid asc  
>   Γçé select "sql" from 
"main".sqlite_master where 
type = 'table' and name = 
'assessment_attempts'  
>   Γçé select name, type, 
not "notnull" as 
"nullable", dflt_value as 
"default", pk as 
"primary", hidden as 
"extra" from pragma_table_x
info('assessment_attempts',
 'main') order by cid asc  
>   Γçé select "sql" from 
"main".sqlite_master where 
type = 'table' and name = 
'assessment_attempts'  
>   Γçé select name, type, 
not "notnull" as 
"nullable", dflt_value as 
"default", pk as 
"primary", hidden as 
"extra" from pragma_table_x
info('assessment_attempts',
 'main') order by cid asc  
>   Γçé select "sql" from 
"main".sqlite_master where 
type = 'table' and name = 
'assessment_attempts'  
>   Γçé select name, type, 
not "notnull" as 
"nullable", dflt_value as 
"default", pk as 
"primary", hidden as 
"extra" from pragma_table_x
info('assessment_attempts',
 'main') order by cid asc  
>   Γçé select "sql" from 
"main".sqlite_master where 
type = 'table' and name = 
'assessment_attempts'  
>   Γçé select name, type, 
not "notnull" as 
"nullable", dflt_value as 
"default", pk as 
"primary", hidden as 
"extra" from pragma_table_x
info('assessment_attempts',
 'main') order by cid asc  
>   Γçé select "sql" from 
"main".sqlite_master where 
type = 'table' and name = 
'assessment_attempts'  
>   Γçé select 'primary' 
as name, group_concat(col) 
as columns, 1 as "unique", 
1 as "primary" from 
(select name as col from pr
agma_table_xinfo('assessmen
t_attempts', 'main') where 
pk > 0 order by pk, cid) 
group by name union select 
name, group_concat(col) as 
columns, "unique", origin 
= 'pk' as "primary" from 
(select il.*, ii.name as 
col from pragma_index_list(
'assessment_attempts', 
'main') il, 
pragma_index_info(il.name, 
'main') ii order by 
il.seq, ii.seqno) group by 
name, "unique", "primary"  
>   Γçé select 
group_concat("from") as 
columns, 'main' as 
foreign_schema, "table" as 
foreign_table, 
group_concat("to") as 
foreign_columns, 
on_update, on_delete from 
(select * from pragma_forei
gn_key_list('assessment_att
empts', 'main') order by 
id desc, seq) group by id, 
"table", on_update, 
on_delete  
    Γçé pragma 
foreign_keys  
>   Γçé alter table 
"assessment_attempts" add 
column "user_id" integer  
>   Γçé create table "__tem
p__assessment_attempts" 
("user_id" integer, 
foreign key("user_id") 
references "users"("id") 
on delete cascade)  
>   Γçé insert into "__temp
__assessment_attempts" 
("user_id") select 
"user_id" from 
"assessment_attempts"  
>   Γçé drop table 
"assessment_attempts"  
>   Γçé alter table "__temp
__assessment_attempts" 
rename to 
"assessment_attempts"  
>   Γçé alter table 
"assessment_attempts" add 
column "assessment_id" 
integer  
>   Γçé alter table 
"assessment_attempts" add 
column "score" integer  
>   Γçé alter table 
"assessment_attempts" add 
column "status" varchar 
check ("status" in 
('in_progress', 
'completed', 'failed')) 
not null default 
'in_progress'  
>   Γçé alter table 
"assessment_attempts" add 
column "started_at" 
datetime  
>   Γçé alter table 
"assessment_attempts" add 
column "completed_at" 
datetime  
    Γçé select exists 
(select 1 from 
"main".sqlite_master where 
name = 'user_answers' and 
type = 'table') as 
"exists"  
    Γçé create table 
"user_answers" ("id" 
integer primary key 
autoincrement not null, 
"created_at" datetime, 
"updated_at" datetime)  
    Γçé select name, type, 
not "notnull" as 
"nullable", dflt_value as 
"default", pk as 
"primary", hidden as 
"extra" from pragma_table_x
info('user_answers', 
'main') order by cid asc  
    Γçé select "sql" from 
"main".sqlite_master where 
type = 'table' and name = 
'user_answers'  
    Γçé select name, type, 
not "notnull" as 
"nullable", dflt_value as 
"default", pk as 
"primary", hidden as 
"extra" from pragma_table_x
info('user_answers', 
'main') order by cid asc  
    Γçé select "sql" from 
"main".sqlite_master where 
type = 'table' and name = 
'user_answers'  
    Γçé select 'primary' 
as name, group_concat(col) 
as columns, 1 as "unique", 
1 as "primary" from 
(select name as col from pr
agma_table_xinfo('user_answ
ers', 'main') where pk > 0 
order by pk, cid) group by 
name union select name, 
group_concat(col) as 
columns, "unique", origin 
= 'pk' as "primary" from 
(select il.*, ii.name as 
col from pragma_index_list(
'user_answers', 'main') 
il, 
pragma_index_info(il.name, 
'main') ii order by 
il.seq, ii.seqno) group by 
name, "unique", "primary"  
    Γçé select 
group_concat("from") as 
columns, 'main' as 
foreign_schema, "table" as 
foreign_table, 
group_concat("to") as 
foreign_columns, 
on_update, on_delete from 
(select * from pragma_forei
gn_key_list('user_answers',
 'main') order by id desc, 
seq) group by id, "table", 
on_update, on_delete  
    Γçé pragma 
foreign_keys  
    Γçé alter table 
"user_answers" add column 
"assessment_attempt_id" 
integer  
>   Γçé create table 
"__temp__user_answers" 
("assessment_attempt_id" 
integer, foreign key("asses
sment_attempt_id") 
references "assessment_atte
mpts"("id") on delete 
cascade)  
    Γçé insert into 
"__temp__user_answers" 
("assessment_attempt_id") 
select 
"assessment_attempt_id" 
from "user_answers"  
    Γçé drop table 
"user_answers"  
    Γçé alter table 
"__temp__user_answers" 
rename to "user_answers"  
    Γçé alter table 
"user_answers" add column 
"question_id" integer  
    Γçé alter table 
"user_answers" add column 
"option_id" integer  
    Γçé create unique 
index "roles_name_unique" 
on "roles" ("name")  
    Γçé create table 
"permissions" ("id" 
integer primary key 
autoincrement not null, 
"name" varchar not null, 
"label" varchar, 
"created_at" datetime, 
"updated_at" datetime)  
    Γçé create unique 
index 
"permissions_name_unique" 
on "permissions" ("name")  
    Γçé create table 
"role_user" ("role_id" 
integer not null, 
"user_id" integer not 
null, "created_at" 
datetime, "updated_at" 
datetime, foreign 
key("role_id") references 
"roles"("id") on delete 
cascade, foreign 
key("user_id") references 
"users"("id") on delete 
cascade, primary key 
("role_id", "user_id"))  
    Γçé create table 
"permission_role" 
("permission_id" integer 
not null, "role_id" 
integer not null, 
"created_at" datetime, 
"updated_at" datetime, 
foreign 
key("permission_id") 
references 
"permissions"("id") on 
delete cascade, foreign 
key("role_id") references 
"roles"("id") on delete 
cascade, primary key 
("permission_id", 
"role_id"))  
>   2026_03_13_085523_add_p
assed_to_assessment_attempt
s_table   
>   Γçé alter table 
"assessment_attempts" add 
column "passed" tinyint(1) 
not null default '0'  
    2026_03_14_232904_refin
e_user_roles_and_status   
    Γçé select name, type, 
not "notnull" as 
"nullable", dflt_value as 
"default", pk as 
"primary", hidden as 
"extra" from pragma_table_x
info('users', 'main') 
order by cid asc  
    Γçé select "sql" from 
"main".sqlite_master where 
type = 'table' and name = 
'users'  
    Γçé select 'primary' 
as name, group_concat(col) 
as columns, 1 as "unique", 
1 as "primary" from 
(select name as col from pr
agma_table_xinfo('users', 
'main') where pk > 0 order 
by pk, cid) group by name 
union select name, 
group_concat(col) as 
columns, "unique", origin 
= 'pk' as "primary" from 
(select il.*, ii.name as 
col from 
pragma_index_list('users', 
'main') il, 
pragma_index_info(il.name, 
'main') ii order by 
il.seq, ii.seqno) group by 
name, "unique", "primary"  
    Γçé select 
group_concat("from") as 
columns, 'main' as 
foreign_schema, "table" as 
foreign_table, 
group_concat("to") as 
foreign_columns, 
on_update, on_delete from 
(select * from pragma_forei
gn_key_list('users', 
'main') order by id desc, 
seq) group by id, "table", 
on_update, on_delete  


